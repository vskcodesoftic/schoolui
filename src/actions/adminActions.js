import { toast } from "react-toastify";
import { ADD_USER_ROLE } from "./types";
import { api_url } from "../utils/api";

import axios from 'axios';

export const userLogin = (user, history) => (dispatch) => {

    
}